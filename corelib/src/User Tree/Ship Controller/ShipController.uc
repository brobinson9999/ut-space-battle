class ShipController extends BaseObject;

simulated function vector getDesiredAcceleration(Ship ship, float delta);
simulated function vector getDesiredRotation(Ship ship, float delta);

defaultproperties
{
}